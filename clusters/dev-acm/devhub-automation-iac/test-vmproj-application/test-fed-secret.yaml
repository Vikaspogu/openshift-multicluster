---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: test-fed-secret
  namespace: test-vmproj
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: test-fed-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        password: "{{ .password }}"
  dataFrom:
    - extract:
        key: vm-user-password