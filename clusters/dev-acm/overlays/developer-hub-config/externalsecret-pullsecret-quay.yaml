---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: dockerconfig
  namespace: developer-hub
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: dockerconfig
    creationPolicy: Owner
    template:
      metadata:
        annotations:
          reflector.v1.k8s.emberstack.com/reflection-allowed: "true"
          reflector.v1.k8s.emberstack.com/reflection-allowed-namespaces: "[a-z-0-9]*-gitops"
          reflector.v1.k8s.emberstack.com/reflection-auto-enabled: "true"
          reflector.v1.k8s.emberstack.com/reflection-auto-namespaces: "[a-z-0-9]*-gitops"
      data:
        config.json: "{{ .quay_pull_secret | toString }}"
  dataFrom:
    - extract:
        conversionStrategy: Default
        decodingStrategy: None
        key: redhat-pull-secret
        metadataPolicy: None
