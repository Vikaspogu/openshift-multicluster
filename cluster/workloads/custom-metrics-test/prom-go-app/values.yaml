---
image:
  repository: docker.io/vikaspogu/prom-go-app
  tag: "1.0"
service:
  main:
    ports:
      web:
        port: 8080
    monitor:
      enabled: true
      endpoints:
        - port: web
          scheme: http
          path: /metrics
          interval: 30s
          scrapeTimeout: 10s
