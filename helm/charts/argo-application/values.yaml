# Default values for argo-application.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

cluster:
  type: boostrap
  name: ""
git:
  url: https://github.com/Vikaspogu/openshift-home-cluster.git
  revision: main
