---
apiVersion: hive.openshift.io/v1
kind: ClusterImageSet
metadata:
  name: {{ include "managed-cluster.fullname" . }}-imageset
  labels:
    {{- include "managed-cluster.labels" . | nindent 4 }}
spec:
  releaseImage: {{.Values.cluster.image}}
