cluster:
  inventoryName:
  name:
  image: quay.io/openshift-release-dev/ocp-release:4.13.13-x86_64
  baseDomain:
  labels:
    # sriov: "true"
    # sno: "true"

sshPublicKey:

controlPlaneSize:

networking:
  clusterCidr:
  machineCidr:
  networkType:
  serviceNetwork:
