---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: acm-cluster
  namespace: openshift-gitops
spec:
  generators:
    - git:
        repoURL: https://github.com/Vikaspogu/openshift-home-cluster.git
        revision: "main"
        directories:
          - path: clusters/cluster-overlays/acm/*
  strategy:
    type: RollingSync
    rollingSync:
      steps:
        - matchExpressions:
            - key: type
              operator: In
              values:
                - operator
                - chart
        - matchExpressions:
            - key: type
              operator: In
              values:
                - config
        - matchExpressions:
            - key: type
              operator: In
              values:
                - app
        - matchExpressions:
            - key: type
              operator: In
              values:
                - cluster
  goTemplate: true
  template:
    metadata:
      name: '{{ .path.basename }}'
      labels:
        type: '{{ .path.basename | splitList "-" | last }}'
    spec:
      destination:
        server: "https://kubernetes.default.svc"
      project: default
      syncPolicy:
        automated: {}
        retry:
          limit: 10
          backoff:
            duration: "120s"
            factor: 5
            maxDuration: "10m0s"
        syncOptions:
          - SkipDryRunOnMissingResource=true
          - Validate=false
          - ApplyOutOfSyncOnly=true
          - RespectIgnoreDifferences=true
      source:
        path: "{{.path.path}}"
        repoURL: "https://github.com/Vikaspogu/openshift-home-cluster.git"
        targetRevision: "main"
