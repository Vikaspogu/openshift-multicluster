---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: team-thor-application-set
  namespace: team-thor-shared
spec:
  generators:
    - git:
        repoURL: https://github.com/idp-team/team-a-gitops
        revision: HEAD
        files:
          - path: "kustomize/cluster-overlays/pxm-acm/team-thor/*.yaml"
  goTemplate: true
  goTemplateOptions:
    - missingkey=error
  template:
    metadata:
      name: 'team-thor-{{.path.filename | splitList "-" | first }}'
    spec:
      destination:
        server: 'https://kubernetes.default.svc'
      project: default
      sources:
        - chart: common
          helm:
            valueFiles:
              - '$values/{{ .path.path }}/{{.path.filename}}'
          repoURL: 'https://vikaspogu.dev/helm-charts/'
          targetRevision: 1.10.0
        - ref: values
          repoURL: 'https://github.com/idp-team/team-a-gitops'
          targetRevision: main
