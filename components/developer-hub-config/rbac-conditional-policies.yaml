result: CONDITIONAL
roleEntityRef: role:default/team2
pluginId: catalog
permissionMapping:
  - read
  - update
  - delete
conditions:
  rule: IS_ENTITY_OWNER # replace with label, not create or update
  resourceType: catalog-entity
  params:
    claims:
      - "$currentUser"
# allOf, anyOf, not
