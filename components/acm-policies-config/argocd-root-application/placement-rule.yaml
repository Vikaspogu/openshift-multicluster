---
apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: argocd-root-application-placement
  namespace: open-cluster-management
spec:
  # This will go to all managed clusters
  clusterConditions:
    - status: "True"
      type: ManagedClusterConditionAvailable
