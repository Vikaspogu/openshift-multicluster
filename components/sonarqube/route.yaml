---
kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: sonarqube
  labels:
    app: sonarqube
spec:
  to:
    kind: Service
    name: sonarqube
    weight: 100
  port:
    targetPort: 9000-tcp
  tls:
    termination: edge
  wildcardPolicy: None
